import React from 'react';
import BottomNavBar from '../../components/BottomNavBar/BottomNavBar';

const ProfileScreen = () => {
    return (
        <div style={{ padding: '20px', paddingBottom: '80px' }}>
            <h1>Perfil</h1>
            <p>Tela de perfil em construção.</p>
            <BottomNavBar />
        </div>
    );
};

export default ProfileScreen;
